/**
 * 应用根组件（占位）。
 * 当前为 Model 层框架（frontend/src/types + frontend/src/api），
 * 页面与路由骨架后续在 pages/constants 中补充。
 */
export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gray-50 text-gray-700">
      <p>个人博客系统前端 · Model 层框架已就绪</p>
    </main>
  )
}
